# PocketBase backend URL. Leave empty to use same-origin relative calls
# (which is what docker compose and `npm run dev` both rely on — nginx and
# the Vite dev proxy respectively route /api and /_/ to PB). Override only
# to point the bundle at a remote PocketBase instance.
VITE_PB_URL=
